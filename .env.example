# Public origin used for absolute URLs in metadata, sitemap and generated agent
# context. Optional on Vercel: falls back to VERCEL_PROJECT_PRODUCTION_URL /
# VERCEL_URL, then http://localhost:3000. A bare host name is treated as https.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
